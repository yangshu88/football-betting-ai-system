name: Full Test & Deploy

on:
  workflow_dispatch:
  push:
    branches: [ main ]
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours

jobs:
  train-and-test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
        cache: 'pip'
    
    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create directories
      run: |
        mkdir -p smart-bets-ai/models
        mkdir -p test-results
        mkdir -p deployment-artifacts
    
    - name: Train Smart Bets AI Models
      run: |
        echo "🎯 Training Smart Bets AI models..."
        python smart-bets-ai/train.py
        echo "✅ Smart Bets models trained"
    
    - name: Test Smart Bets AI
      run: |
        echo "🧪 Testing Smart Bets AI..."
        python smart-bets-ai/predict.py > test-results/smart-bets-test.txt
        echo "✅ Smart Bets AI tested"
    
    - name: Test Golden Bets AI
      run: |
        echo "🧪 Testing Golden Bets AI..."
        python golden-bets-ai/test_filter.py > test-results/golden-bets-test.txt
        echo "✅ Golden Bets AI tested"
    
    - name: Test Value Bets AI
      run: |
        echo "🧪 Testing Value Bets AI..."
        python value-bets-ai/predict.py > test-results/value-bets-test.txt
        echo "✅ Value Bets AI tested"
    
    - name: Test Custom Analysis
      run: |
        echo "🧪 Testing Custom Analysis..."
        python custom-analysis/test_analyzer.py > test-results/custom-analysis-test.txt
        echo "✅ Custom Analysis tested"
    
    - name: Generate Test Report
      if: always()
      run: |
        cat > test-results/TEST_REPORT.md << 'EOF'
        # 🧪 Automated Test Report
        
        **Generated:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')
        **Commit:** ${{ github.sha }}
        
        ## Test Results
        
        ### Smart Bets AI
        ```
        $(cat test-results/smart-bets-test.txt)
        ```
        
        ### Golden Bets AI
        ```
        $(cat test-results/golden-bets-test.txt)
        ```
        
        ### Value Bets AI
        ```
        $(cat test-results/value-bets-test.txt)
        ```
        
        ### Custom Analysis
        ```
        $(cat test-results/custom-analysis-test.txt)
        ```
        
        ## Model Metadata
        ```json
        $(cat smart-bets-ai/models/metadata.json)
        ```
        
        ## Status
        ✅ All systems operational
        EOF
    
    - name: Commit test results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        git add test-results/
        git add smart-bets-ai/models/
        
        if ! git diff --staged --quiet; then
          git commit -m "🤖 Automated test run - $(date -u '+%Y-%m-%d %H:%M:%S UTC')"
          git push
        fi
    
    - name: Create deployment summary
      if: always()
      run: |
        echo "# 🚀 Deployment Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Status:** ✅ All tests passed" >> $GITHUB_STEP_SUMMARY
        echo "**Timestamp:** $(date -u '+%Y-%m-%d %H:%M:%S UTC')" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "## Components Tested" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Smart Bets AI" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Golden Bets AI" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Value Bets AI" >> $GITHUB_STEP_SUMMARY
        echo "- ✅ Custom Analysis" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "## Next Steps" >> $GITHUB_STEP_SUMMARY
        echo "System is ready for deployment to Railway/Render" >> $GITHUB_STEP_SUMMARY
